Numeric Public CHWC_VALVE_CTRL, HWC_VALVE_CTRL
Numeric Input ALARM_POWER_LOSS


Goto ALARM_MONITOR

ALARM_MONITOR:
  If (ALARM_POWER_LOSS)  then Goto PowerLoss
  Goto WAITLOOP

PowerLoss:

  CHWC_VALVE_CTRL = 100
  HWC_VALVE_CTRL = 100
 
  Goto WAITLOOP

WAITLOOP:
if  ts > 5 then goto ALARM_MONITOR