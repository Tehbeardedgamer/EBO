NUMERIC INPUT AIRFLOW, FLOW_SP
NUMERIC OUTPUT ALARM_AIRFLOW_LOW
NUMERIC TMR
FUNCTION SECTIMER

IF SECTIMER((AIRFLOW < (FLOW_SP * .7)),900,TMR) THEN ALARM_AIRFLOW_LOW = 1 ELSE ALARM_AIRFLOW_LOW = 0
